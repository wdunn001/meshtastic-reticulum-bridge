# Minimal broker for the bridge: a Meshtastic node uplinks here, the bridge
# subscribes. Anonymous by default for easy setup on a trusted LAN. For anything
# exposed, add a password file (mosquitto_passwd) and set allow_anonymous false,
# then set MQTT_USERNAME / MQTT_PASSWORD for the bridge to match.
listener 1883
allow_anonymous true
persistence false
